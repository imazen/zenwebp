# Tuned-default adoption round 3: acting on the Pareto review (2026-07-16)

Follow-up to `rd_pareto_review_2026-07-16.md` — the review's levers, tried
on the tuned default (`CostModel::ZenwebpDefault`; parity untouched and
byte-verified per change). Harness: `dev/tuned_ab_sweep.rs` (now with
`--parity` and `--encoder libwebp` arms); corpus: the 15-image A/B set;
metric: matched-zsim size ratio (log-interp), `< 1` = candidate wins.
Raw data: `rd_round3_chroma_2026-07-16.tsv`.

## 1. libwebp-exact chroma at m1+ → **ADOPTED** (`6ef3df9b`)

The tuned gamma-corrected `TunedByteRound` chroma (adopted on synthetic
low-q evidence) measured as a matched-zsim LOSS on real content at every
method. Switching the tuned default to `ChromaPrec::LibwebpExact`:

| m | matched-zsim ratio vs old tuned |
|---|---|
| 0 | 0.9996 (neutral) |
| 1 | **0.9985** |
| 2 | **0.9970** |
| 4 | **0.9976** (low-q **0.9932**) |
| 6 | 0.9996 |

Adopted at method >= 1. m0 keeps `TunedByteRound`: it gains nothing there
(0.9996) and it is what holds the synthetic gradient m0-q10 zensim floor
(69.29 vs the 70 floor under exact chroma) — so the draft tier is
bit-unchanged (confirmed 1.0000) and no test floor moves. The zensim
regression matrix passes 14/14 as-is.

## 2. Segment-quant via libm pow (truncate-exact) → **REJECTED (null)**

The round-2 reasoning ("±1 quant index at a rare boundary, direction-free
noise") is confirmed by measurement: 1.0000 / 1.0000 / 0.9997 / 0.9999 at
m0/2/4/6. Not worth the libm cost on the tuned path.

## 3. m2 UV-RD pick (spend the speed surplus on chroma RD) → **REJECTED**

Replacing m2's SSE UV refine with the full `pick_best_uv` RD path:
matched-zsim **1.0002** (null) at **+24.7% encode time**. The SSE refine
already lands on the RD-equivalent chroma modes at this tier; the m2
speed surplus should not be spent here. (This also bounds the "partial RD
at m2" family: the remaining m2 gap is not chroma-mode selection.)

## Where the tuned default now sits vs libwebp (matched-zsim)

| m | before round 3 | after |
|---|---|---|
| 0 | 0.910 | **0.908** |
| 2 | 1.010 | **1.005** |
| 4 | 0.997 | **0.993** |
| 6 | 0.988 | **0.984** |

m2's residual −0.5%: quant rounding measured null, chroma now adopted,
UV modes ruled out — what remains is skip-proba estimation and SSE-path
tie-break details (each reasoned or measured sub-0.1%) plus the
structural absence of any m3+-style benefit at the tier. Further m2 work
means importing RD scoring for the I16-vs-I4 luma cut, which is the m3
tier's identity — recorded as a deliberate non-goal for now (the tier
ladder exists; m2 is within 0.5% of libwebp while being faster).

Remaining open item from the review: the scans-class pocket (+3.3% at
RD tiers) — needs a per-MB decision diff vs libwebp before any knob can
be proposed.
